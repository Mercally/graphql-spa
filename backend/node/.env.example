# MongoDB connection shared with the .NET backend (same instance/database).
MONGODB_CONNECTION_STRING=mongodb://localhost:27017
MONGODB_DATABASE=workmanagement

# Port for this Node.js backend (REST + GraphQL on the same server instance).
PORT=4000
