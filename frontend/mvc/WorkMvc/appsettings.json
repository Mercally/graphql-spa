{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "Backend URLs are normally supplied via the DOTNET_API_URL / DOTNET_GRAPHQL_URL / NODE_API_URL / NODE_GRAPHQL_URL environment variables (see root .env.example). BackendUrls below is only a fallback for local dev when those env vars are not set.",
  "BackendUrls": {
    "DotnetApiUrl": "http://localhost:5000",
    "DotnetGraphQlUrl": "http://localhost:5000/graphql",
    "NodeApiUrl": "http://localhost:4000",
    "NodeGraphQlUrl": "http://localhost:4000/graphql"
  }
}
